# rusty-claw

Rust port of the Claude Code agent harness architecture — command/tool inventories, prompt routing, turn-based session management.

```
cargo build
cargo run -- summary
cargo run -- commands --limit 5
cargo run -- route "review MCP tool"
cargo test
```
